from itertools import *
for i in count(3):
    print(i)

    if i >= 20:
        break"""

"""from itertools import *
numbers = list(accumulate(range(8))) #accumulate = keeps on adding numbers
print(numbers)